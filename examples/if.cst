// if statements allow you to only run
// code if the top stack value is true

10 bind age

age 20 >= if
	"you are at least 20" print
else age 15 >= if
	"you are between 15 and 20 years old" print
else
	"you are under 15" print
endif
